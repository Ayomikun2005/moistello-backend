linters:
  disable:
    - errcheck
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gofmt

issues:
  # Don't fail on a few remaining warnings — let CI be green while code matures
  max-issues-per-linter: 0
  max-same-issues: 0
